[pytest]
markers =
    benchmark: acceptance and performance benchmark validations
    unit: fully isolated unit tests (no I/O, no network)
    intg: integration tests requiring live Kuzu on temp disk
    e2e: end-to-end tests with live Kuzu and mocked Gemini
    perf: performance benchmark tests measured in-process
